# 이 파일을 복사해 ai/.env를 만들고 실제 값을 채워 넣는다. .env는 gitignore 대상이므로 커밋하지 않는다.

# OpenAI (analysis/llm.py가 AI 종목분석 서술(llmNarrative)을 생성할 때 사용).
# 값이 없어도 /ai/stocks/{symbol}/analysis 자체는 동작하고 llmNarrative만 null로 남는다.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
